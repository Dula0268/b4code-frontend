/**
 * CalendarGrid Component
 *
 * Shared calendar grid used by availability views to render
 * date cells with booking status, pricing, and selection controls.
 */
export default function CalendarGrid() {
  return (
    <div className="p-4 border border-dashed border-gray-300 rounded-xl">
      <h3 className="m-0 font-semibold">features/owner/availability/calendar-grid</h3>

      {/* TODO: Replace with real interactive calendar grid */}
      <p className="mt-2 text-gray-500">
        Placeholder component. Replace with real UI.
      </p>
    </div>
  );
}
